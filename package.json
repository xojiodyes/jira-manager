{
  "name": "jira-manager",
  "version": "0.1.0",
  "description": "Web UI for Jira task management",
  "main": "server.js",
  "scripts": {
    "start": "MODE=real node server.js",
    "mock": "MODE=mock node server.js",
    "dev": "MODE=mock node --watch server.js",
    "migrate": "node migrations/migrate.js"
  },
  "keywords": [
    "jira",
    "task-management",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
